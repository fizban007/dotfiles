(defvar c++-ide-use-google-c-style nil
  "If non nil the package google-c-style will be loaded and enforced.")

(defvar c++-ide-build-command "ninja"
  "This is the default build command for test cases, mapped to <F6> on the keyboard.")
